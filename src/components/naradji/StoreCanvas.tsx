/**
 * @deprecated Use `NaradjiVoiceLayer` — ambient overlay on the Payload ecommerce shell.
 * Kept as a thin re-export so older imports do not break mid-refactor.
 */
export { NaradjiVoiceLayer as StoreCanvas } from './NaradjiVoiceLayer'
